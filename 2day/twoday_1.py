# 사용자의 시선 : 컵 안에 물이 있다 -> 컵을 들고 마신다.
# 개발자의 시선 : cup(변수) 물(값 value)
# 모든 움직이는 것은 함수(메소드)
# decimal : 10진수 0~10,11,12 ... -> 사칙연산 된다.
# "문자열" or '문자열'. : 일반적인 타입 -> 사칙연산 안된다.
# 쌍따옴표 : 더블쿼터(doublequtter) 홀따옴표 : 싱글쿼터(singlequtter)

#변수는 변할 수 있다. 상수는 변할 수 없다.
#오른쪽은 값(value), 왼쪽은 변수. 오른쪽의 값이 변수에 대입된다.
#error(오류) python은 빨간색이 나오면 오류이다.
#빨간색 없으면 오류가 아니다. 정규 표현식에 맞다.

cup='물'
cup2="삼다수"


print(cup)
print(cup2)

#type() : 타입을 출력하는 명령어 *
#str(ing) : 문자열로 변환
#object : object -> string,int,float
#object가 제일 큰 자료형. -> 모든 타입을 가질 수 있다.

print(type(str(cup)))

name='kim chaeseong'
print(name)


